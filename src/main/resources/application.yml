spring:
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
     url: jdbc:mysql://localhost:3306/carsharing
     username: root
     password: root
     driver-class-name: com.mysql.jdbc.Driver